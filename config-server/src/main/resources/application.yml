spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/hemant097/microservice-centralized-config-server
          username: hemant097
          password: ${GITHUB_PAT}
          default-label: main

server:
  port: 8888

#eureka-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka




