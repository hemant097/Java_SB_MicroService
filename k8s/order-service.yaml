apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
spec:
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: noviceanemone/ecommerce-learn-order-service:latest
          resources:
            limits:
              memory: "400Mi"
              cpu: "200m"
            requests:
              memory: "200Mi"
              cpu: "100m"
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: DB_USER
              value: "user"
            - name: DB_PASSWORD
              value: "root"
            - name: DB_SERVICE
              value: "order-db-service"
            - name: CONFIG_SERVER_URL
              value: "http://config-service:8888"
          ports:
            - containerPort: 9020


---

apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  selector:
    app: order-service
  ports:
    - port: 80
      targetPort: 9020
